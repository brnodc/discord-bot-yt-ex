# Copie para .env e preencha. Nunca commite .env.
# Formato: CHAVE=valor (sem espaços em volta do =).
BOT_TOKEN=
CHANNEL_ID=
# Opcional — se vazio, o script usa o TARGET_URL por defeito em discord-yt-bot.py
# TARGET_URL=https://www.exponent.finance/farm/fragsol-10Jul25
