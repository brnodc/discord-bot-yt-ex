__pycache__/
*.pyc
*.pyo
*.pyd

# Segredos — o Git ignora apenas ficheiros não rastreados. Se .env já estiver no
# histórico, pare de o rastrear com: git rm --cached .env
.env
.env.local
.env.*.local
